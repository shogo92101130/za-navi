-- =========================================================
-- 既存DB向け修正スクリプト（2026-06-10）
--
-- 不具合：三田 1F Aエリアの座席が A-02 から始まっており、A-01 が
--         存在しない（他のすべてのエリアは XX-01 から始まっている）。
-- 原因  ：初期データ投入時に seat_id=1 の行が欠落していた。
-- 対応  ：seat_id=1 として A-01（三田・1F・A）を追加する。
--
-- 適用方法：MySQLクライアントで
--   USE zaseki1;
--   SOURCE migration_2026-06-10_fix_mita_1f_a_seat01.sql;
-- のように実行する。
-- =========================================================

INSERT INTO seats (seat_id, base_name, f_name, area_name, seat_name)
VALUES (1, '三田', '1F', 'A', 'A-01');
